<%= form_with(model: conta) do |form| %>
  <% if conta.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(conta.errors.count, "error") %> prohibited this conta from being saved:</h2>

      <ul>
        <% conta.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :descricao, style: "display: block" %>
    <%= form.text_field :descricao %>
  </div>

  <div>
    <%= form.label :valor_inicial, style: "display: block" %>
    <%= form.text_field :valor_inicial %>
  </div>

   <div class="form-group">
    <%= form.label :cor_fundo, "Cor de Fundo" %>
    <%= form.color_field :cor_fundo, class: 'form-control' %>
  </div>
  
  <div class="form-group">
    <%= form.label :cor_fonte, "Cor da Fonte" %>
    <%= form.color_field :cor_fonte, class: 'form-control' %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
