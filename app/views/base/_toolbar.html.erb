<!-- Topbar -->
<nav class="navbar navbar-expand sidebar-black topbar mb-4 static-top shadow">
  <div class="container-fluid">
    <!-- System Name -->
    <div class="navbar-brand sidebar-black .sidebar-item">
      <h4>ADV-MONEY</h4>
    </div>

    <!-- User Control -->
    <ul class="navbar-nav ml-auto">
      <% if user_signed_in? %>
        <li class="nav-item dropdown no-arrow">
          <button class="btn btn-warning btn-sm dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
           <i class="fas fa-user-circle fa-sm fa-fw mr-2 text-white"></i>
            <span class="mr-2 d-none d-lg-inline text-white small"><%= current_user.email %></span>
          </button>
          <!-- Dropdown - User Information -->
          <div class="dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="userDropdown">
          <%= link_to edit_user_registration_path, class: 'dropdown-item' do %>
                <i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>
              Perfil
        <% end %>
             <div class="dropdown-divider"></div>
          <%= link_to destroy_user_session_path, data: { turbo_method: :delete }, class: 'dropdown-item' do %>
            <i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>
            Sair
            <% end %>
          <% if current_user&.admin? %>
           <div class="dropdown-divider"></div>
           Usuários
           <div class="dropdown-divider"></div>
            <%= link_to new_user_registration_path, class: 'dropdown-item' do %>
               <i class="fas fa-user-plus mr-2 text-gray-400"></i>
               Adicionar
            <% end %>
          <% end %>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to 'Logar', new_user_session_path, class: 'btn btn-primary' %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
<!-- End of Topbar -->

